/* import React from 'react'

export const helpers = () => {
  return (
    <div>helpers</div>
  )
}

export const GetUniqueProperty = (array,property) =>{
    return [...new Set(array.map(element => element[property]))]; 
}

*/